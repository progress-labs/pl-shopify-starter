{% # Section Rendering API shim — fetched by the cart islands (cart-drawer.ts, cart-drawer-items.ts) to re-render the drawer after cart updates. Not merchant-facing; do not delete. %}
{%- render 'cart-drawer' -%}
