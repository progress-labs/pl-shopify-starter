{% doc %}
  Renders a collection card

  @param {string} [sizes] - forwarded to the image block; set from grid contexts
{% enddoc %}

{%- liquid
  assign card_collection = block.settings.collection
-%}

<div class="relative" {{ block.shopify_attributes }}>
  <a href="{{ card_collection.url }}" class="absolute inset-0 overflow-hidden">
    <span class="sr-only">{{ card_collection.title }}</span>
  </a>
  <div class="grid gap-4">
    <div class="aspect-[3/2] overflow-hidden rounded bg-primary/5">
      {%- content_for 'block', type: 'image', id: 'image', sizes: sizes -%}
    </div>
    {%- content_for 'block', type: 'heading', id: 'heading' -%}
  </div>
</div>

{% schema %}
{
  "name": "Collection card",
  "tag": null,
  "blocks": [{ "type": "image" }, { "type": "heading" }],
  "settings": [
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    }
  ],
  "presets": [
    {
      "name": "Collection card",
      "settings": {}
    }
  ]
}
{% endschema %}
