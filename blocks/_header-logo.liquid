{%- doc -%}
  Displays a logo in the header.
{%- enddoc -%}

{%- if request.page_type == 'index' -%}
  <h1 class="w-full lg:w-auto">
{%- endif %}
<a
  href="{{ routes.root_url }}"
  class="flex h-full w-full flex-grow items-center justify-center self-stretch font-heading leading-[3rem] font-heading-weight md:leading-[4rem] lg:inline lg:h-auto lg:w-auto lg:leading-none"
  {{ block.shopify_attributes }}
>
  {{ shop.name }}
</a>
{%- if request.page_type == 'index' -%}
  </h1>
{%- endif %}

{% schema %}
{
  "name": "Logo",
  "tag": null
}
{% endschema %}
